# CLAUDE.md

## プロジェクト概要

**プロジェクト名:** bichiku-list  
**目的:** （ここにプロジェクトの目的を記載）

---

## Git 運用ルール

### 基本方針

コードを変更するたびに、必ず GitHub へプッシュする。

### 手順

1. **変更を加える**
2. **差分を確認する**
   ```bash
   git status
   git diff
   ```
3. **ステージングとコミット**
   ```bash
   git add <変更ファイル>
   git commit -m "変更内容を端的に記述したメッセージ"
   ```
4. **GitHub へプッシュ**
   ```bash
   git push origin <ブランチ名>
   ```

> コードを1行でも変更したら、その都度コミット＆プッシュを行うこと。  
> まとめてプッシュは禁止。変更の追跡性を担保するため、小さな単位でコミットする。

### コミットメッセージ規則

```
<種別>: <変更内容の要約（日本語可）>

例:
feat: ユーザー一覧画面を追加
fix: ログインエラー時のメッセージを修正
refactor: 備蓄リスト取得処理をリファクタリング
docs: READMEにセットアップ手順を追加
```

| 種別 | 用途 |
|------|------|
| `feat` | 新機能 |
| `fix` | バグ修正 |
| `refactor` | リファクタリング |
| `docs` | ドキュメント変更 |
| `test` | テスト追加・修正 |
| `chore` | ビルド・設定変更 |

### ブランチ戦略

```
main          ← 本番リリース用（直接プッシュ禁止）
develop       ← 開発の統合ブランチ
feature/xxx   ← 機能開発ブランチ（develop からブランチを切る）
fix/xxx       ← バグ修正ブランチ
```

---

## 開発環境

（使用言語・フレームワーク・ツールをここに記載）

---

## セットアップ

（セットアップ手順をここに記載）

---

## テスト

（テスト実行方法をここに記載）
